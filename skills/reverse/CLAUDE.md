# reverse

A minimal example skill that reverses a string. Useful as the smallest reference for the SKILL.md format the loader expects.

## Code in this directory

- `SKILL.md` — frontmatter (`name`/`description`/`usage`) plus a short snippet; demonstrates the minimal shape `skills_loader._load_skills()` parses
